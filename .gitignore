# 테라폼 상태 파일 (민감정보 포함 가능)
*.tfstate
*.tfstate.backup

# 테라폼 변수 override (종종 민감정보 포함)
*.tfvars
*.tfvars.json

# 테라폼 작업 디렉토리
.terraform/

# crash log, 실행 중 생성될 수 있음
crash.log

# override 파일 - 환경마다 다를 수 있음
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI 환경 설정 캐시
.terraform.rc
terraform.rc

# ───────────── Terraform 관련 ─────────────
*.tfstate
*.tfstate.backup
.terraform/
.crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform.rc
.terraform.rc
*.tfvars
*.tfvars.json

# .terraform.lock.hcl은 커밋하는 게 좋음 (버전 고정)

# ───────────── Terragrunt 관련 ─────────────
# terragrunt가 생성하는 다운로드 캐시 (모듈, provider 등)
.terragrunt-cache/

# Terragrunt에서 임시 복사 디렉토리로 쓰는 곳 (상황에 따라 다름)
**/.terragrunt-cache/

# Terragrunt에서 만든 .hcl 파일 복사본들 (특정 상황에서)
*.auto.tfvars.json

# Terragrunt 실행 후 생기는 임시 plan 파일 (원하면 무시)
*.plan


# tfenv에서 설치한 바이너리 (버전별 terraform)
.terraform-version
.terraform.lock.hcl # 커밋 권장 (버전 고정용)

# tfenv 자체의 설치 디렉토리 무시 (보통 홈 디렉토리에 있음)
.terraform.d/
tfenv/
